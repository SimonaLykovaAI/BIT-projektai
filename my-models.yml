models:
  gemini-2.5-flash-preview-05-20:
    provider: google
    # Do NOT store secrets directly in repository files.
    # The AI Toolkit / your runtime should read the key from the environment.
    # Replace GOOGLE_API_KEY in your `.env` or environment with the valid key.
    api_key: "${GOOGLE_API_KEY}"